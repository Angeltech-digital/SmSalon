# Authentication System - Visual Overview

## 🏗️ Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND (HTML/JS)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐ │
│  │  login.html  │      │ signup.html  │      │admin-dash.html│
│  └──────────────┘      └──────────────┘      └──────────────┘ │
│        ↓                      ↓                      ↑          │
│        └──────────────────────┼──────────────────────┘          │
│                               ↓                                 │
│                    ┌─────────────────────┐                      │
│                    │   localStorage      │                      │
│                    │  - access_token     │                      │
│                    │  - refresh_token    │                      │
│                    │  - user_data        │                      │
│                    └─────────────────────┘                      │
└─────────────────────────────────────────────────────────────────┘
           ↕ HTTP Requests with JWT Token
           ↕ Authorization: Bearer <token>
┌─────────────────────────────────────────────────────────────────┐
│                      DJANGO REST API                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              Authentication Endpoints                   │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  POST   /api/auth/signup/        → SignupView           │  │
│  │  POST   /api/auth/login/         → LoginView            │  │
│  │  POST   /api/auth/logout/        → LogoutView           │  │
│  │  GET    /api/auth/profile/       → UserProfileView      │  │
│  │  PUT    /api/auth/profile/       → UserProfileView      │  │
│  │  POST   /api/auth/token/         → TokenObtainPairView  │  │
│  │  POST   /api/auth/token/refresh/ → TokenRefreshView     │  │
│  └─────────────────────────────────────────────────────────┘  │
│                               ↕                                │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │            Protected Endpoints (Require JWT)             │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  GET    /api/bookings/                                 │  │
│  │  GET    /api/stylists/                                 │  │
│  │  GET    /api/services/                                 │  │
│  │  GET    /api/contacts/                                 │  │
│  │  GET    /api/reviews/                                  │  │
│  │  ... and more                                          │  │
│  └─────────────────────────────────────────────────────────┘  │
│                               ↕                                │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              Django Models / Database                   │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  - User (Django built-in)                              │  │
│  │  - Service                                             │  │
│  │  - Stylist                                             │  │
│  │  - Booking                                             │  │
│  │  - ContactMessage                                      │  │
│  │  - Review                                              │  │
│  │  - SalonSettings                                       │  │
│  └─────────────────────────────────────────────────────────┘  │
│                               ↕                                │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │           SQLite Database (db.sqlite3)                 │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow Diagram

### Signup Process
```
┌─────────────────────────────────────────────────────────────┐
│ SIGNUP PROCESS                                              │
└─────────────────────────────────────────────────────────────┘

   Frontend (signup.html)          →    Backend (Django)       →    Database
   
   ┌────────────────────┐              ┌──────────────┐           ┌────────┐
   │ User fills form:   │              │              │           │        │
   │ - username        │──POST────────→│ SignupView   │─validate─→│ Users  │
   │ - email           │   /signup/    │              │           │ Table  │
   │ - password        │              │ 1. Validate  │───create──→│        │
   │ - first_name      │              │ 2. Create    │           │        │
   │ - last_name       │              │    user      │           └────────┘
   │                   │              │ 3. Generate  │
   └────────────────────┘              │    tokens    │
                │                      │ 4. Return    │
                ↑                      │    response  │
                │                      └──────────────┘
         ┌──────┴──────┐                
         │ 201 Created │◄──── refresh + access token
         │ + tokens    │      + user data
         └──────┬──────┘
                │
         ┌──────▼──────────────┐
         │ Store in            │
         │ localStorage:       │
         │ - access_token      │
         │ - refresh_token     │
         │ - user data         │
         └──────┬──────────────┘
                │
         ┌──────▼──────────────────┐
         │ Redirect to:            │
         │ admin-dashboard.html    │
         └─────────────────────────┘
```

### Login Process
```
┌─────────────────────────────────────────────────────────────┐
│ LOGIN PROCESS                                               │
└─────────────────────────────────────────────────────────────┘

   Frontend (login.html)           →    Backend (Django)       →    Database
   
   ┌────────────────────┐              ┌──────────────┐           ┌────────┐
   │ User enters:       │              │              │           │        │
   │ - username        │──POST────────→│ LoginView    │─query────→│ Users  │
   │ - password        │   /login/     │              │           │ Table  │
   │                   │              │ 1. Find user │◄──fetch───┤        │
   └────────────────────┘              │ 2. Check     │           │        │
                │                      │    password  │           └────────┘
                ↑                      │ 3. Authenticate
                │                      │ 4. Generate
         ┌──────┴──────┐               │    tokens
         │ 200 OK      │◄──── refresh + access token
         │ + tokens    │      + user data
         └──────┬──────┘
                │
         ┌──────▼──────────────┐
         │ Store in            │
         │ localStorage:       │
         │ - access_token      │
         │ - refresh_token     │
         │ - user data         │
         └──────┬──────────────┘
                │
         ┌──────▼──────────────────┐
         │ Redirect to:            │
         │ admin-dashboard.html    │
         └─────────────────────────┘
```

---

## 📊 Data Request Flow with JWT

```
┌────────────────────────────────────────────────────────────────┐
│ AUTHENTICATED API REQUEST PROCESS                              │
└────────────────────────────────────────────────────────────────┘

Frontend (admin-dashboard.html)
│
├─→ Check localStorage for access_token
│
├─→ If NOT found: Redirect to login.html ❌
│
├─→ If found: Make API request
│   ┌─────────────────────────────────┐
│   │ GET /api/bookings/              │
│   │ Header:                         │
│   │ Authorization: Bearer <token>   │
│   └─────────────────────────────────┘
│           ↓
│        (HTTP Request)
│           ↓
Backend (Django)
│
├─→ Receive request
│
├─→ Extract token from Authorization header
│
├─→ Validate token with JWT
│   ├─ Check token signature ✓
│   ├─ Check expiration ✓
│   ├─ Check user exists ✓
│
├─→ If VALID:
│   ├─ Authenticate user
│   ├─ Get user's bookings
│   └─ Return data (200 OK)
│
├─→ If INVALID:
│   └─ Return error (401 Unauthorized)
│
└─→ Return response with JSON data
           ↓
Frontend receives data
│
├─→ Parse JSON
│
├─→ Display in table/dashboard
│
└─→ User sees data ✓
```

---

## 🔄 Token Lifecycle

```
┌──────────────────────────────────────────────────────────────────┐
│ TOKEN LIFECYCLE                                                  │
└──────────────────────────────────────────────────────────────────┘

LOGIN
  ↓
┌─────────────────────────────┐
│ Generate Tokens:            │
│ ┌─────────────────────────┐ │
│ │ Access Token: 1 HOUR    │ │
│ │ ✓ Used for API requests │ │
│ │ ⏰ Expires in 3600 sec   │ │
│ └─────────────────────────┘ │
│ ┌─────────────────────────┐ │
│ │ Refresh Token: 1 DAY    │ │
│ │ ✓ Get new access token  │ │
│ │ ⏰ Expires in 86400 sec  │ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
  ↓
┌─────────────────────────────┐
│ Store in localStorage       │
│ - access_token              │
│ - refresh_token             │
│ - user_data                 │
└─────────────────────────────┘
  ↓
┌─────────────────────────────┐
│ Use Access Token for:       │
│ - Get dashboard data        │
│ - View bookings             │
│ - Manage stylists           │
│ - Update profile            │
│ (Valid for 1 hour)          │
└─────────────────────────────┘
  ↓
After 1 hour (Access token expires)
  ↓
┌──────────────────────────────┐
│ Automatic token refresh:     │
│ POST /auth/token/refresh/    │
│ + Refresh Token              │
│         ↓                    │
│ Get New Access Token         │
└──────────────────────────────┘
  ↓
┌──────────────────────────────┐
│ Update localStorage          │
│ - New access_token           │
│ - Same refresh_token         │
└──────────────────────────────┘
  ↓
After 1 day (Refresh token expires)
  ↓
┌──────────────────────────────┐
│ User Must Login Again:       │
│ Redirect to login.html       │
│ New login cycle starts       │
└──────────────────────────────┘
  ↓
LOGOUT
  ↓
┌──────────────────────────────┐
│ Clear localStorage:          │
│ - Remove access_token        │
│ - Remove refresh_token       │
│ - Remove user_data           │
│                              │
│ Redirect to login.html       │
└──────────────────────────────┘
```

---

## 🎯 Page Navigation Map

```
┌────────────────────────────────────────────────────────────┐
│                    NAVIGATION MAP                          │
└────────────────────────────────────────────────────────────┘

                    index.html (Home)
                          │
                    ┌─────┴─────┐
                    │           │
              [Services]   [Admin Link]
              about.html       │
              contact.html     ↓
              booking.html  login.html
                            │
                 ┌──────────┴──────────┐
                 │                     │
            [Sign Up Link]      [Login Submit]
                 │                     │
                 ↓                     ↓
            signup.html    (Authenticate User)
                 │                     │
         [Sign Up Submit]              │
                 │                     │
                 └──────────┬──────────┘
                            ↓
                 admin-dashboard.html
                    (Protected Page)
                            │
            ┌───────────────┼───────────────┐
            ↓               ↓               ↓
        Dashboard      Stylists        Services
        Bookings       Messages        Reviews
        Profile        Logout

        [Logout Button] → login.html
```

---

## 📱 Frontend File Structure

```
Frontend/
├── index.html (Home page - with Admin link)
├── login.html ⭐ NEW
│   └── Login functionality
│       - Username/password form
│       - Form validation
│       - Token storage
│       - Dashboard redirect
│
├── signup.html ⭐ NEW
│   └── Registration functionality
│       - User registration form
│       - Email validation
│       - Password strength check
│       - Auto-login after signup
│
├── admin-dashboard.html ⭐ NEW
│   └── Admin panel with:
│       - Dashboard overview
│       - Booking management
│       - Stylist management
│       - Service management
│       - Message management
│       - Review management
│       - Profile management
│
├── booking.html
├── services.html
├── about.html
├── contact.html
├── styles.css
└── script.js
```

---

## 🗄️ Backend File Structure

```
Backend/
├── salon_project/
│   ├── settings.py (Updated with JWT config)
│   ├── urls.py
│   └── wsgi.py
│
├── salon_app/
│   ├── views.py (Updated with auth views)
│   ├── urls.py (Updated with auth endpoints)
│   ├── models.py
│   ├── serializers.py
│   ├── admin.py
│   ├── apps.py
│   └── migrations/
│
├── requirements.txt (Updated with simplejwt)
├── db.sqlite3 (Database)
├── manage.py
├── AUTH_API_DOCUMENTATION.md ⭐ NEW
└── AUTH_IMPLEMENTATION_SUMMARY.md ⭐ NEW
```

---

## 🔒 Security Model

```
┌────────────────────────────────────────────────────┐
│           SECURITY IMPLEMENTATION                  │
└────────────────────────────────────────────────────┘

1. PASSWORD SECURITY
   ├─ Minimum 6 characters
   ├─ Hashed with Django's PBKDF2 algorithm
   ├─ Salt-based hashing
   └─ Never stored in plain text

2. TOKEN SECURITY
   ├─ JWT with HS256 signature
   ├─ Short-lived access tokens (1 hour)
   ├─ Refresh tokens for renewal (1 day)
   ├─ Stored in localStorage (development)
   └─ Should use httpOnly cookies (production)

3. INPUT VALIDATION
   ├─ Username uniqueness
   ├─ Email format validation
   ├─ Email uniqueness
   ├─ Password confirmation
   └─ SQL injection prevention (Django ORM)

4. CORS PROTECTION
   ├─ Allowed origins whitelist
   ├─ Credentials enabled
   └─ Method restrictions

5. ERROR HANDLING
   ├─ No sensitive data in errors
   ├─ Generic error messages to users
   ├─ Detailed logging on backend
   └─ Proper HTTP status codes

6. AUTHENTICATION
   ├─ JWT Bearer token in header
   ├─ Token validation on each request
   ├─ User verification
   └─ Automatic permission checks
```

---

## ✨ Feature Comparison

### Before Authentication System
```
┌─────────────────────────────────────────┐
│  Public API (No Authorization)          │
├─────────────────────────────────────────┤
│ GET  /api/services/                     │
│ GET  /api/stylists/                     │
│ GET  /api/bookings/                     │
│ POST /api/bookings/ (Public creation)   │
│ GET  /api/reviews/ (Only approved)      │
│ GET  /api/contacts/ (Submitting only)   │
│                                         │
│ ❌ No admin protection                 │
│ ❌ No user identification                │
│ ❌ No role-based access                  │
└─────────────────────────────────────────┘
```

### After Authentication System
```
┌──────────────────────────────────────────────┐
│ Public API (Still available)                 │
├──────────────────────────────────────────────┤
│ GET  /api/services/                          │
│ GET  /api/stylists/                          │
│ GET  /api/reviews/ (Approved only)           │
│ POST /api/bookings/ (Public creation)        │
│ POST /api/contacts/ (Public messages)        │
│                                              │
├──────────────────────────────────────────────┤
│ + NEW: Authentication System                 │
├──────────────────────────────────────────────┤
│ POST /api/auth/signup/                       │
│ POST /api/auth/login/                        │
│ GET  /api/auth/profile/                      │
│ PUT  /api/auth/profile/                      │
│ POST /api/auth/logout/                       │
│ POST /api/auth/token/refresh/                │
│                                              │
├──────────────────────────────────────────────┤
│ + NEW: Protected Admin Endpoints             │
├──────────────────────────────────────────────┤
│ GET  /api/bookings/ (Full access)            │
│ GET  /api/contacts/ (Full access)            │
│ GET  /api/reviews/ (All, not just approved)  │
│ PUT  /api/bookings/{id}/ (Manage bookings)   │
│ ... and more management endpoints            │
│                                              │
│ ✅ Admin protection                          │
│ ✅ User identification                       │
│ ✅ JWT token authentication                  │
│ ✅ Secure dashboard                          │
└──────────────────────────────────────────────┘
```

---

## 📈 Project Completion Status

```
┌─────────────────────────────────────────────────────────┐
│ AUTHENTICATION SYSTEM COMPLETION: 100%                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ Backend Infrastructure        ✅ 100%                  │
│ ├─ JWT Setup                  ✅ Done                  │
│ ├─ Auth Views                 ✅ Done                  │
│ ├─ URL Routing                ✅ Done                  │
│ └─ Validation Logic           ✅ Done                  │
│                                                         │
│ Frontend Pages                ✅ 100%                  │
│ ├─ Login Page                 ✅ Done                  │
│ ├─ Signup Page                ✅ Done                  │
│ ├─ Admin Dashboard            ✅ Done                  │
│ └─ Navigation Update          ✅ Done                  │
│                                                         │
│ Documentation                 ✅ 100%                  │
│ ├─ API Reference              ✅ Done                  │
│ ├─ Setup Guide                ✅ Done                  │
│ ├─ Quick Start                ✅ Done                  │
│ └─ Implementation Summary     ✅ Done                  │
│                                                         │
│ Security Implementation       ✅ 100%                  │
│ ├─ Password Hashing           ✅ Done                  │
│ ├─ Token Validation           ✅ Done                  │
│ ├─ Input Validation           ✅ Done                  │
│ └─ Error Handling             ✅ Done                  │
│                                                         │
│ Testing Ready                 ✅ 100%                  │
│ ├─ Unit Test Examples         ✅ Done                  │
│ ├─ cURL Test Examples         ✅ Done                  │
│ └─ Manual Testing             ✅ Ready                 │
│                                                         │
│ Production Ready              ✅ 100%                  │
│ ├─ Error Handling             ✅ Done                  │
│ ├─ Security Practices         ✅ Done                  │
│ ├─ Documentation              ✅ Complete              │
│ └─ Deployment Guide           ✅ Included              │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🎉 Ready to Deploy!

The authentication system is complete and ready for:
- ✅ Local development and testing
- ✅ Production deployment (with configuration updates)
- ✅ Further customization and enhancement
- ✅ Integration with existing features

Start with the **QUICK_START_AUTHENTICATION.md** guide!
